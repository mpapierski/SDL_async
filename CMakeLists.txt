CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (SDL_async)

FIND_PACKAGE (SDL REQUIRED)
INCLUDE_DIRECTORIES (${SDL_INCLUDE_DIR})
INCLUDE_DIRECTORIES (${CMAKE_CURRENT_SOURCE_DIR}/include)

ADD_LIBRARY (SDL_async
	src/async.c)

ADD_SUBDIRECTORY (examples)